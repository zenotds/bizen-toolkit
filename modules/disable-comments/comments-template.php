<?php
/**
 * Empty comments template.
 *
 * Served through the `comments_template` filter so themes that call
 * comments_template() render nothing while the module is enabled.
 */

defined( 'ABSPATH' ) || exit;
